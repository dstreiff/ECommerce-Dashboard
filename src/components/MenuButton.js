
const MenuButton = ({OpenSideMenu}) => {
    return (
      <div id="menu-btn" onClick = {OpenSideMenu} >
        <span className="material-icons-sharp">menu</span>
      </div>
    )
  }
  
  export default MenuButton